# NOACCESS Blinker

## Overview

This proof of concept demonstrates a cyclic payload protection mechanism using **PAGE_NOACCESS** and **Vectored Exception Handling (VEH)**. Instead of keeping the payload permanently executable in memory, the shellcode is stored in an **AES-encrypted** and **Base64-encoded** form before being protected with **PAGE_NOACCESS**.

When execution of the protected memory is attempted, Windows raises a **STATUS_ACCESS_VIOLATION** exception. The registered VEH intercepts the exception, restores read/write permissions, decrypts and decodes the payload, and changes the protection to executable before resuming execution.

Once the payload has finished executing, it is immediately encoded, encrypted, and protected with **PAGE_NOACCESS** again. This cycle repeats for every execution attempt, allowing the payload to remain in an unreadable state for the majority of its lifetime.

---

# Implementation

The implementation begins by loading an AES-encrypted and Base64-encoded shellcode file into memory. A sufficiently large buffer is allocated using `VirtualAlloc`, after which the encoded payload is copied into the allocated region. A Vectored Exception Handler is then registered and the payload memory is protected using `PAGE_NOACCESS`.

Whenever execution is attempted, the memory protection causes a **STATUS_ACCESS_VIOLATION**. The VEH verifies that the faulting address belongs to the protected payload, temporarily restores read/write access, decrypts the payload using AES, decodes the Base64 representation, and finally changes the protection to `PAGE_EXECUTE_READ` before allowing execution to continue.

After the shellcode thread completes, the payload is transformed back into its protected representation by Base64 encoding, AES encrypting, and restoring `PAGE_NOACCESS`. This sequence is repeated indefinitely, demonstrating a continuous cycle of runtime restoration and protection.

---

# Code Walkthrough

### Loading the protected payload

The proof of concept loads an AES-encrypted and Base64-encoded shellcode file into memory before allocating a larger buffer capable of accommodating decoding and encryption operations.

---

### Registering the exception handler

A Vectored Exception Handler is registered before the payload memory is protected with `PAGE_NOACCESS`. Any attempt to execute the payload will therefore generate an access violation that is redirected to the handler.

---

### Restoring the payload

Upon receiving a `STATUS_ACCESS_VIOLATION`, the VEH verifies that the exception originated from the protected payload. The memory protection is temporarily relaxed, the payload is AES decrypted and Base64 decoded, and executable permissions are restored before resuming execution.

---

### Executing the payload

Once execution resumes, Windows retries the original instruction that generated the access violation. Since the payload is now executable, the shellcode executes normally.

---

### Re-protecting the payload

After execution completes, the payload is immediately Base64 encoded, AES encrypted, and returned to `PAGE_NOACCESS`, restoring the original protected state before the next execution cycle.

---

# Execution Flow

```text
Load Encrypted Payload
        ↓
Allocate Memory
        ↓
Register VEH
        ↓
Protect with PAGE_NOACCESS
        ↓
Attempt Execution
        ↓
STATUS_ACCESS_VIOLATION
        ↓
VEH Triggered
        ↓
PAGE_READWRITE
        ↓
AES Decrypt
        ↓
Base64 Decode
        ↓
PAGE_EXECUTE_READ
        ↓
Resume Execution
        ↓
Execute Shellcode
        ↓
Base64 Encode
        ↓
AES Encrypt
        ↓
PAGE_NOACCESS
        ↓
Repeat
```

---

# Expected Output

During execution the console repeatedly demonstrates the protection cycle:

- Access violation generated by the protected payload.
- Payload restoration through decryption and decoding.
- Successful shellcode execution.
- Re-encryption and re-protection using `PAGE_NOACCESS`.

https://github.com/user-attachments/assets/8c9e4f74-4890-4df5-b21b-43ab8fa729f2

---

# Notes

- Unlike previous proof of concepts that protect payloads only before execution, this implementation repeatedly restores and protects the payload after every execution cycle.
- The payload exists in plaintext only for the duration required to execute.
- The proof of concept combines multiple protection mechanisms, including **AES encryption**, **Base64 encoding**, **PAGE_NOACCESS**, and **Vectored Exception Handling**, to demonstrate cyclic runtime payload restoration.

---

# References

- Microsoft. *AddVectoredExceptionHandler*
- Microsoft. *VirtualProtect*
- Microsoft. *Structured Exception Handling*
- Microsoft. *Cryptography API: Next Generation*